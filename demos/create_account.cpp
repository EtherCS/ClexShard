#include <iostream>
#include <fstream>
#include <sys/types.h>
#include <cstring>
#include <string>
#include <map>
#include <vector>
using namespace std;

void createRandom(const int &length, const int &number) {
    vector<char> temp;
    for (int i = 0; i < number; i++) {
        for (int j = 0; j < length; j++) {
            
        }
    }
}